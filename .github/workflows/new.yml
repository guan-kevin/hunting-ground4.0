on:
  push:
  workflow_dispatch:
    inputs:
      branch:
        required: true
        default: 'reproducer'

jobs:
  test-pipeline:
    name: Test Pipeline
    runs-on: ubuntu-latest
    steps:
      - run: echo "Branch: ${{ inputs.branch }}"
      
