on:
  push:
  workflow_dispatch:
    inputs:
      branch:
        required: true
        default: 'reproducer'

jobs:
  test:
    name: Test Pipeline
    runs-on: [self-hosted, s]
    steps:
      - run: echo "${{ github.workspace }}"
